班级分组第六小组

1、项目分配

	组长：陈芳玲
		管理远程库，指导队员使用git进行版本控制，分配任务，整理文件
		制作页面：购物车 、详情页
   	组员：吴怀宇
	         协作组长利用版本控制项目
		制作页面：首页、列表页
	组员：刘亚飞
		协作组长利用版本控制项目
		制作页面：订单页、完善信息页面、个人中心页
	组员：蒙有才
		协作组长利用版本控制项目
		制作页面：小金库页面、评论页


2、项目目录
	
    app
		img:放置图片文件，如果banner、广告图等

		css:放置css文件
			reset.css	重置性样式（normalize.css）
			common.css	公共模块/重复性类
			index.css
			style.css 等页面样式


		sass:页面中样式用sass编写

		font 字体图标文件

		js:放置js文件
			各个页面书写的文件 如 index.js首页js文件	
		
		assets:放置插件
			框架等css、js 等文件
		
		html
			列表页，商品详情页，购物车等子页面
	       		  index.html:主页
		json   存放模拟的数据 

		dist   构建目录文件（通过文件监听的形式编译sass文件到此目录）

   doc
		放置项目文档说明，项目要求与规范

   .gitignore  过滤文件

   gulpFile.js   监听与编译文件

   package.json 建立依赖文件

3、项目规范

	
	1、所以在项目中：
		1）所有文件统一使用utf-8编码
		2）所有文件命名使用英文字符，包括html文件、css文件、js文件、图片等

	2、HTML规范
		1）利用html5语义化标签编写结构
		2）id,class命名尽量做到见名知意
		3）尽量减少多余嵌套
		4）避免“多类症”
		原则：能不添加类就尽量保持文档结构，此处可跟标签语义化结合
		5）避免“多div症”
		原则：应只在没有现有元素能实现区域分割的情况下使用div

	2、CSS规范
		1）类名、属性名、属性值全部使用小写字母
		2）尽量不要用id去写样式
		考虑样式覆盖的简易性

	3、js 规范
		1）每行代码结束必须有分号
		2）变量、函数使用驼峰式命名
		3）命名语义化, 尽可能利用英文单词或其缩写
		4）格式化代码, 并加适量注释

	4、图片规范
		1）图片格式仅限于gif、png、jpg
		2）命名全部用小写英文字母、数字、下划线组合，其中不得包含汉字、空格、特殊字符
		3）运用css sprite技术集中小的背景图或图标, 减小页面http请求
		4）装饰性图片放在/css/img文件夹下，其他图片放在/img文件夹下


4、技术与要求
	
	利用html5语义化标签编写结构
	利用css3实现布局
	使用sass编写样式
	使用json模拟数据
	使用git进行版本控制与团队协作
	每个队员必须制定各种规范




